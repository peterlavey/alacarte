{
  "name": "alacarte-server",
  "version": "0.1.0",
  "private": "true",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon --inspect index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.3",
    "nodemon": "^3.1.7",
    "supertest": "^7.0.0",
    "vitest": "^2.1.3"
  }
}
