# Server environment configuration

# Port the Express server listens on
PORT=3001

# Storage backend: memory | mongo
USE_DB=mongo

# MongoDB connection (used when USE_DB=mongo)
MONGO_URL=http://localhost:3001
MONGO_DB=alacarte